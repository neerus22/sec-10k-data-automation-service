# SEC 10-K Fetcher Requirements
# Python 3.8+

# HTTP requests
requests>=2.31.0

# PDF conversion from HTML
weasyprint>=60.0
# WeasyPrint dependencies (may need system libraries on some platforms)
# On macOS: brew install pango gdk-pixbuf libffi
# On Ubuntu/Debian: sudo apt-get install python3-cffi python3-brotli libpango-1.0-0 libpangoft2-1.0-0

# FastAPI for REST API service
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0
pytest-asyncio>=0.21.0

# Optional: For better HTML parsing (handled by WeasyPrint internally)
# lxml>=4.9.0  # WeasyPrint uses lxml if available
